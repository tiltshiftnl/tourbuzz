{% extends 'base/base.twig' %}

{% block main %}

<main class="wrapper app-wrapper">

    <section class="buzz">

        {% include 'partials/dagselectie.twig' %}

        <ol class="berichten">
            {% for record in berichten %}

                {% set title %}
                    {% if lang == 'nl' %}{{ record.title }}{% endif %}
                    {% if lang == 'en' %}{{ record.title_en }}{% endif %}
                    {% if lang == 'fr' %}{{ record.title_fr }}{% endif %}
                {% endset %}

                {% set body %}
                    {% if lang == 'nl' %}{{ record.body|raw }}{% endif %}
                    {% if lang == 'en' %}{{ record.body_en|raw }}{% endif %}
                    {% if lang == 'fr' %}{{ record.body_fr|raw }}{% endif %}
                {% endset %}

                <li><a href="/{{ Y }}/{{ m }}/{{ d }}/details#slide{{ loop.index }}" class="slide-link"><span class="nr">{{ loop.index }}</span>{{ title }}</a></li>
            {% endfor %}
        </ol>

        <section class="kaartmap wrapper">
            <img src="{% include 'partials/googlemap.empty.twig' %}" style="width: 100%">
            {% for record in berichten %}
                {% if record.rel_loc is not empty %}
                    {#<a href="details#slide{{ loop.index }}" data-item="{{ loop.index }}" style="top: {{ record.rel_loc.dY }}%; left: {{ record.rel_loc.dX }}%">#}
                    <a href="/{{ Y }}/{{ m }}/{{ d }}/details#slide{{ loop.index }}" style="top: {{ record.rel_loc.dY }}%; left: {{ record.rel_loc.dX }}%">
                        <img src="/img/markers/{{ loop.index }}.png" class="maplink-m">
                    </a>
                {% endif %}
            {% endfor %}
        </section>

    </section>

</main>

{% endblock %}
