
{#
0 - in sleep mode
1 - car detected
2 - truck detected
3 - car in violation
4 - truck in violation
#}

{#
{% set haltestatus %}{% if haltestatus > 0 %}bezet{% else %}vrij{% endif %}{% endset %}
#}

{#<section class="halte-vrij" style="margin: 10px 0; padding: 10px 10px; background: #f8f8f8">
    beta

    <p>{{ __('Halte status') }}: <b>{{ haltestatus }}</b></p>
    <p class="meta">{{ __('Laatste update') }}: {{ now|date('d-m-Y H:i:s') }}
</section>#}

<h3 style="margin: 20px 0 0">Live status <span class="meta" style="background: #888; color: #fff; font-size: 12px; padding: 0px 5px; margin-bottom: 10px; display: inline-block; border-radius: 5px">beta</span></h3>
<section class="halte-vrij" style="margin: 0 0 15px; padding: 10px 10px; background: #f8f8f8">
    <p><b>{{ haltestatus }}</b></p>

    <p class="meta">{{ __('Laatste update') }}: {{ now|date('d-m-Y H:i:s') }}</p>
</section>